server:
  port: 10001
  servlet:
    context-path: /monitoring


logging:
  level:
    root: info
    org.springframework.web: INFO
    org.springframework.security: INFO
    com.feng: debug
#  path: logs
  file: logs/feng-api.log

management:
  endpoints:
    web:
      exposure:
        include: "*"